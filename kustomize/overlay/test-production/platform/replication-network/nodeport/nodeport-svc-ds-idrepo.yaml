apiVersion: v1
kind: Service
metadata:
  name: ds-idrepo-external
  namespace: forgerock

spec:
  type: NodePort

  selector:
    app: ds-idrepo

  ports:

    - name: repl
      port: 8989
      targetPort: 8989
      nodePort: 30989    
