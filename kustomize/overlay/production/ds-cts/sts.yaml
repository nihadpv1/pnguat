apiVersion: apps/v1
kind: StatefulSet

metadata:
  name: ds-cts
  namespace: forgerock

spec:
  replicas: 3

  serviceName: ds-cts

  updateStrategy:
    type: RollingUpdate

  selector:
    matchLabels:
      app: ds-cts

  template:
    metadata:
      labels:
        app: ds-cts

    spec:
      serviceAccountName: forgerock-sa

      terminationGracePeriodSeconds: 120

      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: node-role.kubernetes.io/forgerock
                    operator: Exists

        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: app
                    operator: In
                    values:
                      - ds-cts
              topologyKey: kubernetes.io/hostname

      tolerations:
        - key: dedicated
          operator: Equal
          value: forgerock
          effect: NoSchedule

      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app: ds-cts

      containers:
        - name: ds

          resources:
            requests:
              cpu: "1"
              memory: 4Gi

            limits:
              memory: 6Gi

      initContainers:
        - name: init

          resources:
            requests:
              cpu: 250m
              memory: 1Gi

            limits:
              memory: 2Gi

  volumeClaimTemplates:
    - metadata:
        name: data

      spec:
        accessModes:
          - ReadWriteOnce

        resources:
          requests:
            storage: 100Gi

        storageClassName: fast-ssd-storage