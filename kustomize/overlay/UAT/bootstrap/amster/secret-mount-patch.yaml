apiVersion: batch/v1
kind: Job
metadata:
  name: amster
spec:
  template:
    spec:
      volumes:
      - name: amster-secrets
        secret:
          items:
          - key: ssh-privatekey
            path: id_rsa
          - key: ssh-publickey
            path: id_rsa.pub
